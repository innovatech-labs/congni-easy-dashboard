import React from "react"
import NavBar from "../components/NavBar";

function Result() {
    return (
        <>
            <NavBar />
            <div>This is the page to display the cover letter (to be updated)</div>
        </>
    );
}

export default Result;
